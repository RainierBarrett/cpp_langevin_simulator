sudo: required

language: cpp

before_script:
  - sudo apt-get update -qq
  - sudo apt-get install libboost-random-dev

script: 
  - mkdir build
  - cd build
  - cmake ..
  - cp ../pot_example.txt .
  - cp ../pot_example.txt ./test
  - cp ../pot_example.txt ./Testing
  - make

  - make test
  - make catch

after_success:
  - chmod a+rw src/CMakeFiles/sharedlib.dir/langevin.cpp.gcno
  - make coverage









